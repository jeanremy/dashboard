{% extends 'FlydDashboardBundle::layout.html.twig' %}

{% block title %}
    <title>{{entity.username}}</title>
{% endblock %}

{% block body %}
        
    <a href="{{ path('user_edit', { 'id': entity.id }) }}" class="btn btn--edit"></a>
    <h1 class="lvl1">{{entity.username}}</h1>

    {% include "FlydDashboardBundle::flash.html.twig" %}


    <div class="row">
        <div class="col-sm-12">
            <div class="bloc bloc--general">
                <h2 class="bloc__title">Informations générales</h2>

                <div class="form-group">
                    
                    <label class="control-label">Fonction</label>

                    <div class="data">
                        {{ entity.job }}
                    </div>

                </div>

                <div class="form-group">
                    
                    <label class="control-label">Email</label>

                    <div class="data">
                        <p>{{ entity.email }}</p>
                    </div>
                    
                </div>


                <div class="form-group">
                    <label class="control-label">Profil</label>
                    <div class="data">
                        <p>{{ entity.image.webPath }}</p>
                    </div>
                    
                </div>

            </div>
        </div>
    </div>

{% endblock %}
